//use std::process::Command;

fn main() {
    println!("Hello, world!");
    // let mut cmd = Command::new("powershell");
    // cmd.args(&["-Command", "ls"]);
    // let res = cmd.output();
    // println!("{:#?}", res);
}
